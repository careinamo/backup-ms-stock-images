import { ResquestDropboxApi } from '../../domain/Clients/ResquestDropboxApi';

export class ApiDropboxClient implements ResquestDropboxApi {
  async searchImagesByPagination(clientId: number): Promise<any> {
    return '';
  }
}
